# SOLAPI 카카오 알림톡 템플릿 — 투어 예약

> **등록 위치**: SOLAPI 콘솔 → 카카오 알림톡 → 템플릿 관리 → 신규 등록
> 카카오톡 채널이 SOLAPI에 연결되어 있어야 합니다.
> 등록 후 검수 승인까지 영업일 기준 1~2일 소요됩니다.

---

## 등록할 템플릿 2종

| 코드 | 용도 | 수신자 | 환경변수 |
|---|---|---|---|
| TOUR_BOOKING_HOST | 새 투어 예약 — 호스트 알림 | 투어 등록한 호스트 본인 | `SOLAPI_TPL_TOUR_BOOKING_HOST` |
| TOUR_BOOKING_GUEST | 새 투어 예약 — 예약자 확인 | 예약자 (게스트) | `SOLAPI_TPL_TOUR_BOOKING_GUEST` |

> **예약자에게는 폰 번호를 입력한 경우에만** 발송됩니다.
> SOLAPI 템플릿 ID가 없으면 **순수 SMS** 로 fallback 발송됩니다 (이 경우 `.env.local` 에 `SOLAPI_TPL_TOUR_BOOKING_GUEST` 미설정 가능).
> 알림톡으로 보냈는데 예약자가 카카오 미가입/차단 상태면 **SMS로 자동 fallback** (SOLAPI ATA failover).

---

## 1. TOUR_BOOKING_HOST (호스트용)

받는 사람: 투어 등록자 본인 — `User.phone` 으로 발송

**SOLAPI 콘솔 입력**:
- 카테고리: 서비스이용 → 이용안내/공지
- 유형: 기본형
- 템플릿명: `tour_booking_host`

**본문**:

```
[void anchae 투어]
#{업체명}님,
새 투어 예약이 들어왔습니다.

▣ 투어
#{투어명}

▣ 일정
#{투어일} #{투어시간}
코스: #{코스시간}

▣ 예약자
#{예약자명} (#{예약자연락처})
인원: #{예약인원}
결제: #{총금액}

▣ 모임장소
#{모임장소}

관리 페이지에서 예약을 확인하세요.
```

**버튼** (선택, 권장):
- 종류: 웹링크
- 이름: `대시보드 열기`
- URL: `https://voidanchae.com/admin/tour-bookings`

**변수 목록**:

| 변수 | 예시 |
|---|---|
| `#{업체명}` | 호스트 displayName 또는 email |
| `#{투어명}` | 북촌 한복투어 |
| `#{투어일}` | 2026-05-10 (일) |
| `#{투어시간}` | 14:00 |
| `#{코스시간}` | 60분 |
| `#{예약자명}` | 홍길동 |
| `#{예약자연락처}` | 010-1234-5678 *또는* `연락처 미입력` |
| `#{예약인원}` | 2명 |
| `#{총금액}` | 100,000원 |
| `#{모임장소}` | 안국역 2번 출구 |

> 게스트가 폰 번호를 입력하지 않은 경우 `#{예약자연락처}` 자리에 `연락처 미입력` 이 들어갑니다.

---

## 2. TOUR_BOOKING_GUEST (예약자용)

받는 사람: 예약자 — `TourBooking.phone` 으로 발송 (입력했을 때만)

**SOLAPI 콘솔 입력**:
- 카테고리: 서비스이용 → 이용안내/공지
- 유형: 기본형
- 템플릿명: `tour_booking_guest`

**본문**:

```
[void anchae 투어 예약 확인]
#{예약자명}님, 예약이 접수되었습니다.

▣ 투어
#{투어명}

▣ 일정
#{투어일} #{투어시간}
코스: #{코스시간}

▣ 인원
#{예약인원}

▣ 모임장소
#{모임장소}

▣ 결제 예정 금액
#{총금액}

운영업체 확정 후 다시 안내드리겠습니다.
문의가 있으시면 회신 부탁드립니다.
```

**버튼** (선택):
- 종류: 웹링크
- 이름: `투어 페이지`
- URL: `https://voidanchae.com/tours`

**변수 목록**:

| 변수 | 예시 |
|---|---|
| `#{예약자명}` | 홍길동 |
| `#{투어명}` | 북촌 한복투어 |
| `#{투어일}` | 2026-05-10 (일) |
| `#{투어시간}` | 14:00 |
| `#{코스시간}` | 60분 |
| `#{예약인원}` | 2명 |
| `#{총금액}` | 100,000원 |
| `#{모임장소}` | 안국역 2번 출구 |

---

## 등록 절차 (SOLAPI 콘솔)

1. **카카오 알림톡** → **템플릿 관리** → **+ 새 템플릿** 클릭
2. **카카오톡 채널 선택**
3. 위 본문 복사 → 붙여넣기 (변수는 `#{}` 그대로)
4. **저장 후 검수 요청** — 1~2 영업일 내 승인
5. 승인되면 SOLAPI 콘솔에서 **템플릿 ID** (예: `KA01TP240501123456789012345`) 복사
6. `.env.local` 에 추가:

   ```ini
   SOLAPI_TPL_TOUR_BOOKING_HOST=KA01TP...
   SOLAPI_TPL_TOUR_BOOKING_GUEST=KA01TP...
   ```

7. dev/prod 서버 재시작 → 발송 활성화

---

## 카카오톡 검수 통과 팁

| 자주 반려되는 사유 | 해결 |
|---|---|
| 광고성 표현 (`최저가`, `최고`, `이벤트` 등) | 제거 |
| 이모지/특수문자 과다 사용 | 본문에서 제거 (▣ 정도는 무방) |
| 변수 형태 오류 (`#{변수}` 외 사용) | `#{}` 형식 준수 |
| 변수 1개 = 본문 전체인 경우 | 고정 텍스트와 변수를 섞기 |

---

## 발송 동작 매트릭스 (예약자 알림 기준)

| 상황 | 결과 |
|---|---|
| 게스트가 폰 미입력 | 미발송 (skip) |
| 폰 입력 + 알림톡 템플릿 등록 + 카카오 가입자 | 알림톡 발송 |
| 폰 입력 + 알림톡 템플릿 등록 + 카카오 미가입/차단 | SMS 자동 fallback (ATA failover) |
| 폰 입력 + 알림톡 템플릿 미등록 | 순수 SMS 발송 |
| 폰 입력 + SOLAPI 자격증명 미설정 | 콘솔 로그만 (`[notify/noop]`) |

호스트 알림은 폰 등록 + 템플릿 등록 + SOLAPI 설정 모두 갖춰져야 발송됩니다 (SMS fallback 없음).

---

## 발송 테스트

승인 + env 설정 후:

1. 시크릿 창에서 `/tours/<slug>` 접속 → **본인 폰 번호** 입력해서 예약 1건 생성
2. 입력한 폰으로 카카오톡 또는 SMS 도착 확인
3. 호스트 폰으로도 알림톡 도착 확인 (User.phone 이 등록돼 있어야)

도착 안 하면 dev 콘솔에서 다음 중 하나 확인:
- `[notify/solapi] ...HTTP 4xx` → 템플릿 ID 또는 변수 누락
- `[notify] ...not set; skipping` → 환경변수 미설정
- `[notify/noop] ...` → SOLAPI 자격증명 미설정 (`SOLAPI_API_KEY` 등)

---

## 코드 참조

- 템플릿 변수 정의: [lib/notify.ts](lib/notify.ts)
- 발송 호출 지점:
  - [app/api/public/tour-bookings/route.ts](app/api/public/tour-bookings/route.ts) — 게스트 예약
  - [app/api/tour-bookings/route.ts](app/api/tour-bookings/route.ts) — 어드민 수동 예약
